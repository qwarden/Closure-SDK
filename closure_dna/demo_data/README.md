# Closure DNA Demo Data

This folder contains plain importable datasets for the web workbench. Each dataset has a `manifest.json`, explicit schemas, per-table JSON row files, and a small `queries.json` set for quick loading into the SQL editor.
